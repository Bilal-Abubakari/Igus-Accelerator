export type ConfigCount = { total: number };

export enum ReviewStatus {
    Uploaded = 'Uploaded',
    Unassigned = 'Unassigned',
    Assigned = 'Assigned',
    Approved = 'Approved',
    Rejected = 'Rejected',
}
