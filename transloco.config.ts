import { TranslocoGlobalConfig } from '@jsverse/transloco-utils';

const config: TranslocoGlobalConfig = {
  rootTranslationsPath: 'apps/iaimc-frontend/public/i18n/',
  langs: ['ar', 'de', 'en', 'es', 'fr', 'jp', 'pt', 'ru', 'zh'],
  keysManager: {},
};

export default config;
